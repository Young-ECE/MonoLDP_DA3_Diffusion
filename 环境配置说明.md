# MonoLDP 本地运行环境配置说明

## 📋 环境要求

### 基础环境
- **操作系统**: Linux (推荐 Ubuntu 20.04+)
- **Python**: 3.8+ (推荐 3.10)
- **CUDA**: 11.0+ (用于 GPU 训练)
- **GPU**: NVIDIA GPU，显存 >= 8GB (训练) / 4GB (推理)

### 软件依赖
- Conda (推荐) 或 Python 虚拟环境
- Git

---

## 🚀 快速安装

### 1. 创建 Conda 环境

```bash
# 创建环境（推荐 Python 3.10）
conda create -n monoldp_depth python=3.10
conda activate monoldp_depth
```

### 2. 安装 PyTorch

根据你的 CUDA 版本选择：

```bash
# CUDA 11.8
conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia

# CUDA 12.1
conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia

# 或使用 pip
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

### 3. 安装项目依赖

```bash
# 进入项目目录
cd /home/jingyang/MonoLDP

# 安装基础依赖
pip install -r requirements.txt
```

### 4. 安装 Depth Anything V3 (可选，用于教师模型)

```bash
# 设置 HuggingFace 镜像（国内用户推荐）
export HF_ENDPOINT=https://hf-mirror.com

# 安装 Depth Anything V3
pip install git+https://github.com/ByteDance-Seed/Depth-Anything-3.git
```

### 5. 验证安装

```bash
python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA可用: {torch.cuda.is_available()}')"
```

---

## 📁 数据集准备

### NYU v2 数据集结构

```
nyu_data/
├── nyu2_train/          # 训练数据
└── nyu2_test/           # 测试数据
```

### 数据预处理 (可选)

如果需要提取超像素和线段：

```bash
cd DepthEstimation

# 提取超像素
python preprocess/extract_superpixel.py --data_path /path/to/nyu_data/

# 提取线段（需要 opencv-python < 3.4.6）
pip install "opencv-python<3.4.6"
python preprocess/extract_lineseg.py --data_path /path/to/nyu_data/
```

---

## 🏃 快速开始

### 训练

```bash
cd DepthEstimation

# 基础训练
python train.py --data_path /path/to/nyu_data/

# 使用 Depth Anything V3 作为教师模型
python train.py \
    --data_path /path/to/nyu_data/ \
    --use_diffusion \
    --use_depth_anything_v3 \
    --depth_anything_v3_model DA3Mono-Large \
    --log_dir /path/to/logs
```

### 推理

```bash
python inference_single_image.py \
    --image_path /path/to/image.jpg \
    --load_weights_folder /path/to/model/weights
```

### 评估

```bash
python evaluate_nyu_depth.py \
    --data_path /path/to/nyu_data/ \
    --load_weights_folder /path/to/model/weights
```

---

## ⚙️ 配置说明

主要配置在 `DepthEstimation/options.py` 中，常用参数：

- `--data_path`: 数据集路径
- `--log_dir`: 日志和模型保存路径
- `--batch_size`: 批次大小（默认 8）
- `--learning_rate`: 学习率（默认 1e-4）
- `--num_epochs`: 训练轮数（默认 15）
- `--use_diffusion`: 使用扩散解码器
- `--use_depth_anything_v3`: 使用 DA3 作为教师模型
- `--gpu_id`: 指定 GPU ID

---

## 🔧 常见问题

### 1. CUDA 不可用

```bash
# 检查 CUDA 驱动
nvidia-smi

# 检查 PyTorch CUDA 支持
python -c "import torch; print(torch.cuda.is_available())"
```

### 2. HuggingFace 下载失败

```bash
# 设置镜像
export HF_ENDPOINT=https://hf-mirror.com

# 或在代码中设置（已自动处理）
```

### 3. 内存不足

- 减小 `batch_size`
- 使用更小的模型（如 ResNet18）
- 减少训练尺度 `--scales 0`

### 4. OpenCV 版本冲突

```bash
# 线提取需要旧版本
pip install "opencv-python<3.4.6"

# 其他功能使用新版本
pip install opencv-python>=4.4.0
```

### 5. NumPy 警告

项目已修复 `np.int` 弃用问题，使用 `np.int32`。

---

## 📊 监控训练

### TensorBoard

```bash
# 启动 TensorBoard
tensorboard --logdir /path/to/logs/train

# 浏览器访问
# http://localhost:6006
```

### GPU 监控

```bash
# 实时监控 GPU
watch -n 1 nvidia-smi

# 查看训练进程
ps aux | grep train.py
```

---

## 📝 环境变量

```bash
# HuggingFace 镜像（推荐国内用户）
export HF_ENDPOINT=https://hf-mirror.com

# CUDA 设备（可选）
export CUDA_VISIBLE_DEVICES=0
```

---

## 🔗 相关链接

- 项目代码: `/home/jingyang/MonoLDP`
- 数据集: 见 README.md
- 预训练模型: 见 README.md

---

## 💡 提示

1. **首次运行**: 建议先用小数据集测试，确认环境配置正确
2. **GPU 内存**: 如果显存不足，减小 `batch_size` 或使用梯度累积
3. **训练时间**: 完整训练可能需要数小时到数天，建议使用 `screen` 或 `tmux`
4. **日志保存**: 训练日志和模型会自动保存到 `--log_dir` 指定的目录

---

**最后更新**: 2024年

