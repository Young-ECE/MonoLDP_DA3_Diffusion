# 🚀 MonoDiffusion 集成快速参考卡

## 📋 一分钟速览

### 已完成 ✅
- ✅ 复制了 `diffusers/` 模块
- ✅ 创建了 `depth_decoder_diffusion.py`
- ✅ 更新了 `networks/__init__.py`
- ✅ 添加了命令行参数
- ✅ 创建了全套文档

### 待完成 ⚠️
- ⚠️ 修改 `trainer.py` (3个位置)

---

## 🎯 修改 trainer.py 的3个位置

### 📍 位置1：导入（文件顶部）
```python
import torch.nn.functional as F
import os
```

### 📍 位置2：初始化（约74行）
在 `self.models["depth"] = networks.DepthDecoder(...)` **之前**添加：

```python
if self.opt.use_diffusion:
    # 教师模型
    self.models["pre_depth_encoder"] = networks.ResnetEncoder(...)
    self.models["pre_depth_decoder"] = networks.DepthDecoder(...)
    # 加载权重 + 冻结
    # ...
    # 学生模型
    self.models["depth"] = networks.DepthDecoderDiffusion(...)
else:
    self.models["depth"] = networks.DepthDecoder(...)  # 原来的代码
```

### 📍 位置3：前向传播（process_batch方法）
在深度预测部分：

```python
# 生成归一化坐标
norm_pix_coords = {...}

if self.opt.use_diffusion:
    # 教师推理
    with torch.no_grad():
        pre_outputs = self.models["pre_depth_decoder"](...)
    # 学生推理
    gt_diff = {("disp_diffusion", s): pre_outputs[("disp", s)] for s in scales}
    outputs = self.models["depth"](features, norm_pix_coords, gt_diff)
else:
    outputs = self.models["depth"](features, norm_pix_coords)

# 损失计算
losses = self.compute_losses(inputs, outputs)
if self.opt.use_diffusion:
    losses['l1'] = ...
    losses['ddim'] = ...
    losses["loss"] = photometric + l1 + ddim
```

---

## 💻 训练命令

### 阶段1：基础模型
```bash
python train.py --model_name base --num_epochs 15
```

### 阶段2：扩散模型
```bash
python train.py --model_name diffusion --use_diffusion \
    --teacher_weights_folder ./logs/base/models/weights_14 \
    --batch_size 6
```

---

## 📚 文档速查

| 需求 | 文档 |
|------|------|
| **快速开始** | `QUICK_START.md` ⭐⭐⭐ |
| **完整指南** | `集成总结.md` |
| **代码示例** | `TRAINER_MODIFICATIONS.md` |
| **项目报告** | `README_DIFFUSION.md` |
| **文件清单** | `FILES_SUMMARY.txt` |

---

## 🔧 快速排错

| 问题 | 解决 |
|------|------|
| `ImportError: timm` | `pip install timm` |
| CUDA OOM | `--batch_size 4` |
| 找不到教师权重 | 检查路径是否正确 |
| Loss不下降 | 降低学习率到1e-5 |

---

## ✅ 检查清单

- [ ] 能成功导入 `DepthDecoderDiffusion`
- [ ] 修改了 `trainer.py` 的3个位置
- [ ] 训练了基础模型
- [ ] 有教师模型权重

---

## 🎓 核心原理

```
输入 → 教师(冻结) → 伪GT
         ↓
      学生编码器
         ↓
    扩散解码器 (3个尺度)
    Scale2 → Scale1 → Scale0
         ↓
    精炼深度图
```

**三个损失**：
- 光度损失：重投影
- L1损失：与教师一致
- DDIM损失：扩散去噪

---

## 📞 最重要的提示

1. **必须先训练基础模型** → 作为教师
2. **减小batch size** → 扩散占用更多显存
3. **监控TensorBoard** → 查看所有损失
4. **参考QUICK_START.md** → 最详细的步骤

---

祝成功！🎉

