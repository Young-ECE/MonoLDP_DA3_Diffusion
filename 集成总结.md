# MonoDiffusion 扩散模块集成到 MonoLDP - 完整指南

## 📋 目录
1. [已完成的工作](#已完成的工作)
2. [关键文件说明](#关键文件说明)
3. [核心概念](#核心概念)
4. [手把手使用教程](#手把手使用教程)
5. [训练流程详解](#训练流程详解)
6. [需要的后续修改](#需要的后续修改)
7. [常见问题](#常见问题)

---

## 已完成的工作

### ✅ 1. 复制了必要的文件
```
MonoDiffusion/diffusers/  →  DepthEstimation/diffusers/
```
包含DDIM调度器，这是扩散模型的核心组件。

### ✅ 2. 创建了扩散深度解码器
新文件：`DepthEstimation/networks/depth_decoder_diffusion.py`

这个文件包含：
- **DepthDecoderDiffusion**: 主解码器类，集成了扩散模块
- **ScheduledCNNRefine**: 扩散模型的噪声预测网络
- **CNNDDIMPipeline_{0,1,2}**: 三个尺度的DDIM推理管道

### ✅ 3. 更新了网络导入
修改：`DepthEstimation/networks/__init__.py`
```python
from .depth_decoder_diffusion import DepthDecoderDiffusion
```

### ✅ 4. 添加了命令行参数
修改：`DepthEstimation/options.py`
新增参数：
- `--use_diffusion`: 是否使用扩散模块
- `--teacher_weights_folder`: 教师模型权重路径
- `--diffusion_steps`: 推理步数 [5, 4, 3]
- `--diffusion_timesteps`: 训练时间步 [250, 200, 150]
- `--diffusion_l1_weight`: L1损失权重
- `--diffusion_ddim_weight`: DDIM损失权重

### ✅ 5. 创建了详细文档
- `DIFFUSION_INTEGRATION_GUIDE.md`: 详细的集成指南
- `TRAINER_MODIFICATIONS.md`: trainer.py的修改说明
- `集成总结.md`: 本文档

---

## 关键文件说明

### 📁 目录结构
```
DepthEstimation/
├── diffusers/              # [新增] 扩散调度器
│   ├── __init__.py
│   └── schedulers/
│       ├── __init__.py
│       └── scheduling_ddim.py
├── networks/
│   ├── __init__.py         # [已修改] 添加了 DepthDecoderDiffusion
│   ├── depth_decoder.py    # [原有] 基础深度解码器
│   └── depth_decoder_diffusion.py  # [新增] 带扩散的深度解码器
├── options.py              # [已修改] 添加了扩散相关参数
├── trainer.py              # [待修改] 需要添加扩散训练逻辑
└── train.py                # [无需修改]
```

### 📄 核心文件功能

#### 1. `depth_decoder_diffusion.py`
**功能**: 集成扩散模块的深度解码器

**关键类**:
- `DepthDecoderDiffusion`: 主解码器
  - 输入：encoder特征 + 归一化像素坐标 + 教师模型的伪GT
  - 输出：精炼后的深度图 + 中间过程 + DDIM损失
  
- `ScheduledCNNRefine`: 噪声预测网络
  - 输入：带噪声的深度图 + 时间步 + 条件特征
  - 输出：预测的噪声

- `CNNDDIMPipeline`: 推理管道
  - 从高斯噪声开始
  - 迭代去噪得到最终深度图

**工作流程**:
```
编码器特征 → 解码器上采样 → 生成条件特征 → 扩散精炼
                                              ↓
                               [Scale 2] → [Scale 1] → [Scale 0]
                                粗糙        中等        精细
```

#### 2. `scheduling_ddim.py`
**功能**: DDIM调度器，控制扩散过程

**核心方法**:
- `add_noise()`: 训练时添加噪声
- `step()`: 推理时去噪一步
- `set_timesteps()`: 设置推理步数

---

## 核心概念

### 🔄 什么是扩散模型？
扩散模型将深度估计视为**去噪过程**：

1. **前向过程**（训练）：向清晰的深度图逐步添加噪声
2. **反向过程**（推理）：从噪声中逐步恢复清晰的深度图

```
清晰深度图 ──添加噪声──→ 纯噪声
                     ↑
                训练学习去噪
                     ↓
清晰深度图 ←──去噪过程── 纯噪声
```

### 🎯 为什么使用扩散模型？
1. **更精确的边缘**: 扩散过程保留细节
2. **不确定性估计**: 多次采样可以估计预测的不确定性
3. **渐进式精炼**: 从粗到细逐步改进

### 🏗️ MonoDiffusion的架构设计

```
输入图像
   ↓
编码器 (ResNet)
   ↓
传统解码器 → 条件特征
   ↓
扩散模块:
  Scale 2 (粗糙, 1/4分辨率) → 初始噪声
     ↓ (上采样)
  Scale 1 (中等, 1/2分辨率)
     ↓ (上采样)
  Scale 0 (精细, 全分辨率) → 最终深度图
```

### 🔑 三个关键损失

1. **光度损失** (Photometric Loss)
   - 原有的重投影一致性损失
   - 确保几何一致性

2. **L1损失** (Consistency Loss)
   - 学生模型的预测 vs 教师模型的预测
   - 确保不偏离太远

3. **DDIM损失** (Diffusion Loss)
   - 扩散模型的去噪损失
   - 学习正确的去噪过程

```python
total_loss = 1.0 * photometric_loss + 
             1.0 * l1_loss + 
             1.0 * ddim_loss
```

---

## 手把手使用教程

### 📝 步骤1：检查安装
```bash
cd /home/jingyang/MonoLDP/DepthEstimation

# 检查文件是否存在
ls diffusers/schedulers/scheduling_ddim.py
ls networks/depth_decoder_diffusion.py

# 测试导入
python -c "from networks import DepthDecoderDiffusion; print('✓ 成功导入')"
python -c "from diffusers.schedulers.scheduling_ddim import DDIMScheduler; print('✓ 成功导入')"
```

### 📝 步骤2：修改 trainer.py

你需要在 `trainer.py` 中做以下修改：

#### 修改点1: 导入必要的模块（文件开头）
```python
import torch.nn.functional as F
import os
```

#### 修改点2: 在 `__init__` 方法中初始化模型（约第74行）

**找到这段代码**:
```python
self.models["depth"] = networks.DepthDecoder(
    self.models["encoder"].num_ch_enc, 
    self.opt.scales,
    PixelCoorModu = not self.opt.disable_pixel_coordinate_modulation
)
```

**替换为**:
```python
# 如果使用扩散模块
if self.opt.use_diffusion:
    print("=" * 60)
    print("使用扩散深度解码器")
    print("=" * 60)
    
    # 创建教师模型（冻结）
    self.models["pre_depth_encoder"] = networks.ResnetEncoder(
        self.opt.num_layers, self.opt.weights_init == "pretrained"
    )
    self.models["pre_depth_decoder"] = networks.DepthDecoder(
        self.models["pre_depth_encoder"].num_ch_enc, 
        self.opt.scales,
        PixelCoorModu = not self.opt.disable_pixel_coordinate_modulation
    )
    
    # 加载教师模型权重
    if self.opt.teacher_weights_folder is not None:
        teacher_path = self.opt.teacher_weights_folder
        encoder_path = os.path.join(teacher_path, "encoder.pth")
        decoder_path = os.path.join(teacher_path, "depth.pth")
        
        if os.path.exists(encoder_path) and os.path.exists(decoder_path):
            print(f"→ 加载教师模型: {teacher_path}")
            encoder_dict = torch.load(encoder_path)
            decoder_dict = torch.load(decoder_path)
            
            model_dict = self.models["pre_depth_encoder"].state_dict()
            depth_model_dict = self.models["pre_depth_decoder"].state_dict()
            
            self.models["pre_depth_encoder"].load_state_dict(
                {k: v for k, v in encoder_dict.items() if k in model_dict}
            )
            self.models["pre_depth_decoder"].load_state_dict(
                {k: v for k, v in decoder_dict.items() if k in depth_model_dict}
            )
            print("✓ 教师模型加载成功")
        else:
            print(f"⚠ 警告: 教师权重未找到于 {teacher_path}")
            print("→ 教师模型将从头训练")
    
    # 冻结教师模型
    for param in self.models["pre_depth_encoder"].parameters():
        param.requires_grad = False
    for param in self.models["pre_depth_decoder"].parameters():
        param.requires_grad = False
    
    self.models["pre_depth_encoder"].to(self.device)
    self.models["pre_depth_decoder"].to(self.device)
    self.models["pre_depth_encoder"].eval()
    self.models["pre_depth_decoder"].eval()
    
    # 创建学生模型（带扩散）
    self.models["depth"] = networks.DepthDecoderDiffusion(
        self.models["encoder"].num_ch_enc, 
        self.opt.scales,
        num_output_channels=3,
        PixelCoorModu = not self.opt.disable_pixel_coordinate_modulation
    )
    print("=" * 60)
else:
    # 不使用扩散，使用原始解码器
    self.models["depth"] = networks.DepthDecoder(
        self.models["encoder"].num_ch_enc, 
        self.opt.scales,
        PixelCoorModu = not self.opt.disable_pixel_coordinate_modulation
    )
```

#### 修改点3: 修改 `process_batch` 方法中的前向传播

**找到生成 norm_pix_coords 的代码**（在process_batch方法中），通常在调用深度解码器之前。

**在深度解码器调用之前添加**:
```python
# 生成归一化像素坐标
norm_pix_coords = {}
for scale in self.opt.scales:
    h = self.opt.height // (2 ** scale)
    w = self.opt.width // (2 ** scale)
    
    y_coords = torch.linspace(0, 1, h).view(h, 1).repeat(1, w)
    x_coords = torch.linspace(0, 1, w).view(1, w).repeat(h, 1)
    coords = torch.stack([x_coords, y_coords], 0)
    coords = coords.unsqueeze(0).repeat(inputs[("color", 0, 0)].shape[0], 1, 1, 1)
    norm_pix_coords[scale] = coords.to(self.device)

# 如果使用扩散，先用教师模型生成伪GT
if self.opt.use_diffusion:
    with torch.no_grad():
        pre_features = self.models["pre_depth_encoder"](inputs["color_aug", 0, 0])
        pre_outputs = self.models["pre_depth_decoder"](pre_features, norm_pix_coords)
    
    # 学生模型前向传播
    features = self.models["encoder"](inputs["color_aug", 0, 0])
    
    # 准备扩散的伪GT
    gt_for_diffusion = {}
    for scale in self.opt.scales:
        gt_for_diffusion[("disp_diffusion", scale)] = pre_outputs[("disp", scale)].detach()
    
    # 使用扩散解码器
    outputs = self.models["depth"](features, norm_pix_coords, gt_for_diffusion)
    
    # 保存教师预测用于损失计算
    for scale in self.opt.scales:
        outputs[("predisp", scale)] = pre_outputs[("disp", scale)]
else:
    # 不使用扩散
    features = self.models["encoder"](inputs["color_aug", 0, 0])
    outputs = self.models["depth"](features, norm_pix_coords)
```

#### 修改点4: 修改 `process_batch` 方法的损失计算

**找到损失计算的代码** (通常在 process_batch 最后):
```python
losses = self.compute_losses(inputs, outputs)
```

**在这之后添加**:
```python
# 如果使用扩散，添加额外损失
if self.opt.use_diffusion:
    # L1损失
    l1_loss = 0
    for scale in self.opt.scales:
        l1_loss += F.l1_loss(outputs[("predisp", scale)], outputs[("disp", scale)])
    losses['l1'] = l1_loss / len(self.opt.scales)
    
    # DDIM损失
    if ("ddim_loss", 0) in outputs:
        ddim_loss = 0
        for scale in self.opt.scales:
            if ("ddim_loss", scale) in outputs:
                ddim_loss += outputs[("ddim_loss", scale)]
        losses['ddim'] = ddim_loss / len(self.opt.scales)
    else:
        losses['ddim'] = torch.tensor(0.0).to(self.device)
    
    # 保存原始光度损失
    losses['photometric'] = losses["loss"]
    
    # 总损失
    losses["loss"] = (1.0 * losses['photometric'] + 
                     self.opt.diffusion_l1_weight * losses['l1'] + 
                     self.opt.diffusion_ddim_weight * losses['ddim'])
```

### 📝 步骤3：训练基础模型（教师模型）

首先需要训练一个基础模型作为教师：

```bash
cd /home/jingyang/MonoLDP/DepthEstimation

# 阶段1：训练基础模型（不使用扩散）
python train.py \
    --model_name monoldp_base \
    --data_path /path/to/your/data \
    --num_epochs 20 \
    --batch_size 12 \
    --learning_rate 1e-4 \
    --height 256 \
    --width 320 \
    --log_dir ./logs
```

训练完成后，模型会保存在：
```
./logs/monoldp_base/models/weights_19/
├── encoder.pth
├── depth.pth
├── pose_encoder.pth
└── pose.pth
```

### 📝 步骤4：使用扩散模块训练（学生模型）

使用训练好的基础模型作为教师：

```bash
# 阶段2：使用扩散模块微调
python train.py \
    --model_name monoldp_diffusion \
    --use_diffusion \
    --teacher_weights_folder ./logs/monoldp_base/models/weights_19 \
    --data_path /path/to/your/data \
    --num_epochs 10 \
    --batch_size 6 \
    --learning_rate 5e-5 \
    --diffusion_steps 5 4 3 \
    --diffusion_l1_weight 1.0 \
    --diffusion_ddim_weight 1.0 \
    --height 256 \
    --width 320 \
    --log_dir ./logs
```

**参数说明**:
- `--use_diffusion`: 启用扩散模块
- `--teacher_weights_folder`: 教师模型路径
- `--diffusion_steps 5 4 3`: 三个尺度的推理步数
- `--batch_size 6`: 扩散模块需要更多显存，减小batch size
- `--learning_rate 5e-5`: 微调使用更小的学习率

### 📝 步骤5：监控训练

使用TensorBoard查看训练过程：

```bash
tensorboard --logdir=./logs --port=6006
```

在浏览器打开 `http://localhost:6006`，你应该能看到：

**标量图表**:
- `loss`: 总损失
- `photometric`: 光度损失
- `l1`: L1一致性损失
- `ddim`: 扩散损失

**图像可视化**:
- `disp_0`: 学生模型预测的深度
- `predisp_0`: 教师模型预测的深度
- `residual_0`: 残差图（教师 - 学生）
- `re-diffusion_*`: 扩散过程的中间结果
- `uncertainty_0`: 不确定性估计

---

## 训练流程详解

### 🔄 完整的训练流程

```
Step 1: 输入图像 (color_aug, 0, 0)
   ↓
Step 2: 教师编码器（冻结）
   ↓
Step 3: 教师解码器 → 伪GT深度图
   ↓
Step 4: 学生编码器（训练）
   ↓
Step 5: 学生解码器 + 扩散模块（训练）
   │
   ├─→ Scale 2: 粗糙深度 (1/4分辨率)
   │      ├─ DDIM Pipeline: 噪声 → 深度
   │      └─ DDIM Loss
   │
   ├─→ Scale 1: 中等深度 (1/2分辨率)
   │      ├─ 使用 Scale 2 作为条件
   │      ├─ DDIM Pipeline: 噪声 → 深度
   │      └─ DDIM Loss
   │
   └─→ Scale 0: 精细深度 (全分辨率)
          ├─ 使用 Scale 1 作为条件
          ├─ DDIM Pipeline: 噪声 → 深度
          └─ DDIM Loss
   ↓
Step 6: 计算损失
   ├─ 光度损失（重投影一致性）
   ├─ L1损失（与教师的一致性）
   └─ DDIM损失（扩散去噪）
   ↓
Step 7: 反向传播更新参数
```

### 📊 损失权重调整建议

**初始设置**（默认）:
```python
photometric_weight = 1.0
l1_weight = 1.0
ddim_weight = 1.0
```

**如果训练不稳定**:
```python
photometric_weight = 1.0
l1_weight = 0.5      # 降低
ddim_weight = 0.5    # 降低
```

**如果边缘不够清晰**:
```python
photometric_weight = 1.0
l1_weight = 0.5
ddim_weight = 1.5    # 增加
```

---

## 需要的后续修改

### ⚠️ 仍需手动修改的文件

#### 1. `trainer.py` - 必须修改
按照"步骤2"中的说明修改以下部分：
- [ ] 模型初始化（`__init__` 方法）
- [ ] 前向传播（`process_batch` 方法）
- [ ] 损失计算（`process_batch` 方法末尾）

#### 2. `trainer.py` - 可选修改

**日志可视化** (`log` 方法):
```python
# 在 log 方法中添加
if self.opt.use_diffusion and s == 0:
    # 教师预测
    if ("predisp", s) in outputs:
        writer.add_image(
            "teacher_pred_{}/{}".format(s, j),
            normalize_image(outputs[("predisp", s)][j].data), 
            self.step
        )
    
    # 残差
    if ("predisp", s) in outputs and ("disp", s) in outputs:
        residual = (outputs[("predisp", s)][j] - outputs[("disp", s)][j]).abs()
        writer.add_image(
            "residual_{}/{}".format(s, j),
            normalize_image(residual.data), 
            self.step
        )
```

**保存模型** (`save_model` 方法):
如果使用扩散，还需要保存教师模型：
```python
# 在 save_model 方法中
if self.opt.use_diffusion:
    # 保存教师模型（虽然不训练，但方便后续使用）
    for model_name in ["pre_depth_encoder", "pre_depth_decoder"]:
        if model_name in self.models:
            save_path = os.path.join(save_folder, "{}.pth".format(model_name))
            torch.save(self.models[model_name].state_dict(), save_path)
```

---

## 常见问题

### ❓ Q1: ImportError: No module named 'timm'

**问题**: 导入 `depth_decoder_diffusion.py` 时报错缺少 `timm` 模块

**解决方案**:
```bash
pip install timm
```

### ❓ Q2: 显存不足 (CUDA out of memory)

**问题**: 训练时显存溢出

**解决方案**:
1. **减小batch size**:
   ```bash
   --batch_size 4  # 或更小
   ```

2. **减少扩散步数**:
   ```bash
   --diffusion_steps 3 2 2  # 原本是 5 4 3
   ```

3. **使用梯度累积**（需要修改trainer.py）:
   ```python
   # 在训练循环中
   if (batch_idx + 1) % accumulation_steps == 0:
       self.model_optimizer.step()
       self.model_optimizer.zero_grad()
   ```

### ❓ Q3: 训练损失不下降

**可能原因和解决方案**:

1. **教师模型未正确加载**:
   检查日志是否显示 "✓ 教师模型加载成功"
   
2. **扩散损失过大压倒其他损失**:
   降低DDIM损失权重：
   ```bash
   --diffusion_ddim_weight 0.5
   ```

3. **学习率过大**:
   ```bash
   --learning_rate 1e-5  # 更小的学习率
   ```

### ❓ Q4: 没有预训练的教师模型怎么办？

**方案1: 先训练基础模型**（推荐）
```bash
# 第一步：训练基础模型（20 epochs）
python train.py --model_name base_model --num_epochs 20

# 第二步：使用基础模型作为教师
python train.py --model_name diffusion_model \
    --use_diffusion \
    --teacher_weights_folder ./logs/base_model/models/weights_19
```

**方案2: 自蒸馏**（Self-distillation）
让教师和学生使用相同的初始化，然后在训练过程中，教师始终是学生的滞后版本（EMA）。

这需要修改trainer.py，实现起来较复杂，不推荐初学者使用。

### ❓ Q5: 推理时如何使用扩散模型？

**方案1: 加载完整模型**（推荐）
```python
# 在推理脚本中
from networks import DepthDecoderDiffusion

model = DepthDecoderDiffusion(...)
model.load_state_dict(torch.load("path/to/depth.pth"))
model.eval()

# 推理时不需要GT
with torch.no_grad():
    outputs = model(features, norm_pix_coords, gt=None)
```

**方案2: 只使用训练好的权重，不使用扩散**
如果只想要最终结果，不需要扩散过程：
1. 使用扩散训练得到好的权重
2. 推理时提取深度解码器部分，跳过扩散步骤

### ❓ Q6: 如何调整推理速度？

推理速度主要受扩散步数影响：

**快速模式** (实时应用):
```bash
--diffusion_steps 1 1 1  # 每个尺度只1步
```

**平衡模式** (默认):
```bash
--diffusion_steps 5 4 3
```

**高质量模式** (离线处理):
```bash
--diffusion_steps 10 8 5
```

注意：推理步数可以与训练时不同，但不建议差异太大。

### ❓ Q7: 如何验证集成是否成功？

**快速测试**:
```python
import torch
from networks import DepthDecoderDiffusion

# 创建模型
model = DepthDecoderDiffusion(
    num_ch_enc=[64, 64, 128, 256, 512],
    scales=[0, 1, 2]
)

# 创建假数据
batch_size = 2
features = [
    torch.randn(batch_size, 64, 64, 80),
    torch.randn(batch_size, 64, 32, 40),
    torch.randn(batch_size, 128, 16, 20),
    torch.randn(batch_size, 256, 8, 10),
    torch.randn(batch_size, 512, 4, 5),
]

norm_pix_coords = {
    0: torch.randn(batch_size, 2, 256, 320),
    1: torch.randn(batch_size, 2, 128, 160),
    2: torch.randn(batch_size, 2, 64, 80),
}

gt = {
    ("disp_diffusion", 0): torch.randn(batch_size, 3, 256, 320),
    ("disp_diffusion", 1): torch.randn(batch_size, 3, 128, 160),
    ("disp_diffusion", 2): torch.randn(batch_size, 3, 64, 80),
}

# 前向传播
model.eval()
with torch.no_grad():
    outputs = model(features, norm_pix_coords, gt)

print("✓ 模型前向传播成功！")
print("输出键:", outputs.keys())
```

如果成功，应该看到：
```
✓ 模型前向传播成功！
输出键: dict_keys([('mask', 0), ('disp', 0), ('disp', 1), ('disp', 2), 're-diffusion', ('ddim_loss', 2), ('ddim_loss', 1), ('ddim_loss', 0)])
```

---

## 📚 参考资料

1. **MonoDiffusion 论文**: "MonoDiffusion: Self-Supervised Monocular Depth Estimation Using Diffusion Model"
   - PDF路径: `MonoDiffusion/assets/Shao 等 - 2025 - MonoDiffusion...pdf`

2. **DDIM 论文**: "Denoising Diffusion Implicit Models"
   - arXiv: https://arxiv.org/abs/2010.02502

3. **关键代码参考**:
   - MonoDiffusion trainer: `MonoDiffusion/trainer_df.py`
   - MonoDiffusion decoder: `MonoDiffusion/networks/hr_decoder_diffusion.py`

---

## 🎯 总结

### 已完成✅:
1. 复制了 `diffusers/` 模块
2. 创建了 `depth_decoder_diffusion.py`
3. 更新了 `networks/__init__.py`
4. 添加了命令行参数到 `options.py`
5. 创建了详细的文档

### 待完成❌:
1. 修改 `trainer.py` 的模型初始化
2. 修改 `trainer.py` 的前向传播逻辑
3. 修改 `trainer.py` 的损失计算
4. (可选) 添加日志可视化

### 快速开始🚀:
```bash
# 1. 检查安装
python -c "from networks import DepthDecoderDiffusion; print('成功！')"

# 2. 训练基础模型
python train.py --model_name base --num_epochs 20

# 3. 使用扩散训练
python train.py --model_name diffusion --use_diffusion \
    --teacher_weights_folder ./logs/base/models/weights_19
```

---

## 📞 需要帮助？

如果遇到问题：
1. 检查本文档的"常见问题"部分
2. 查看 `DIFFUSION_INTEGRATION_GUIDE.md` 的详细说明
3. 查看 `TRAINER_MODIFICATIONS.md` 的代码示例

祝你成功集成扩散模块！🎉

