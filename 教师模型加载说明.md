# 教师模型加载说明

## 概述

MonoLDP 训练系统支持三种互斥的教师模型配置（使用扩散模块时）：

1. **Depth Anything V3**（DA3Mono-Large 等）
2. **Depth Anything V1/V2**（Depth-Anything-V2-Small 等）
3. **传统 Encoder-Decoder**（MonoLDP 原始架构）

**重要：这三种配置是互斥的，只能选择其中一种。**

---

## 1. Depth Anything V3 教师模型（推荐）

### 配置参数
```bash
--use_diffusion \
--use_depth_anything_v3 \
--depth_anything_v3_model DA3Mono-Large
```

### 加载的模型组件
✅ **仅加载**: `depth_anything_v3_teacher`
- 从 HuggingFace 或本地路径加载
- 冻结所有参数（不训练）
- 仅用于生成伪深度标签

❌ **不会创建**:
- `pre_depth_encoder`
- `pre_depth_decoder`
- `depth_anything_teacher`

### 工作流程
1. DA3 模型生成深度图
2. 深度图转换为视差（disparity）
3. 视差作为 diffusion 模块的伪 GT
4. 学生模型（encoder + diffusion decoder）学习预测视差

---

## 2. Depth Anything V1/V2 教师模型

### 配置参数
```bash
--use_diffusion \
--use_depth_anything \
--depth_anything_model_type vits \
--depth_anything_version v2
```

### 加载的模型组件
✅ **仅加载**: `depth_anything_teacher`
- 从 HuggingFace 或本地路径加载
- 冻结所有参数

❌ **不会创建**:
- `pre_depth_encoder`
- `pre_depth_decoder`
- `depth_anything_v3_teacher`

---

## 3. 传统 Encoder-Decoder 教师模型

### 配置参数
```bash
--use_diffusion \
--teacher_weights_folder /path/to/pretrained/model
```

（不使用 `--use_depth_anything_v3` 和 `--use_depth_anything`）

### 加载的模型组件
✅ **加载**: 
- `pre_depth_encoder`
- `pre_depth_decoder`

❌ **不会创建**:
- `depth_anything_teacher`
- `depth_anything_v3_teacher`

---

## 模型保存与加载行为

### 保存检查点时 (`save_model`)
- **不保存**教师模型的权重
- 教师模型是预训练的冻结模型，不需要保存
- 只保存可训练的学生模型组件：
  - `encoder`
  - `depth`（diffusion decoder）
  - `scalenet`
  - `regression`
  - `pose_encoder`
  - `pose`, `pose_rec`, `pose_third`

### 加载检查点时 (`load_model`)
- **跳过**教师模型的加载
- 教师模型总是从各自的源重新加载：
  - DA3: 从 HuggingFace 或本地路径
  - DA1/DA2: 从 HuggingFace 或本地路径
  - 传统: 从 `teacher_weights_folder`

---

## 当前训练配置（DA3）

使用 `train_with_da3.sh` 脚本：

```bash
python train.py \
    --model_name da3_teacher_model \
    --use_diffusion \
    --use_depth_anything_v3 \
    --depth_anything_v3_model DA3Mono-Large \
    --data_path /oldisk/home/jingyang/monoldp/datasets/nyu_data \
    --batch_size 2 \
    --num_epochs 15 \
    --learning_rate 5e-5 \
    --diffusion_l1_weight 1.0 \
    --diffusion_ddim_weight 1.0 \
    --debug_save_teacher \
    --log_dir /oldisk/home/jingyang/monoldp/temp/da3_logs
```

### 实际加载的模型
- ✅ `depth_anything_v3_teacher` (DA3Mono-Large) - 教师
- ✅ `encoder` (ResNet18) - 学生
- ✅ `depth` (DepthDecoderDiffusion) - 学生
- ✅ `scalenet`, `regression` - 学生
- ✅ `pose_encoder`, `pose`, `pose_rec`, `pose_third` - 学生

### 标志位
- `self.use_depth_anything_v3_teacher = True`
- `self.use_depth_anything_teacher = False`
- `self.use_traditional_teacher = False`

---

## 训练日志示例

启动时会看到：
```
============================================================
🔄 使用扩散深度解码器
============================================================
📦 使用 Depth Anything V3 作为教师模型
   - 模型: DA3Mono-Large
   - 教师模型：仅加载 depth_anything_v3_teacher
→ Loading Depth Anything V3 model: DA3Mono-Large
  Using HuggingFace endpoint: https://hf-mirror.com
  Model cache directory: /home/jingyang/.cache/huggingface/hub
  ...
✓ Depth Anything V3 教师模型加载成功
   注意：不会创建 pre_depth_encoder/pre_depth_decoder
   注意：不会创建 depth_anything_teacher (V1/V2)
============================================================
✓ 扩散解码器初始化成功
============================================================
```

保存检查点时会看到：
```
→ 跳过教师模型: depth_anything_v3_teacher（教师模型不保存到检查点）
```

---

## 常见问题

### Q: 为什么教师模型不保存？
**A**: 教师模型是预训练的冻结模型，参数不变。每次训练启动时从源重新加载即可，无需保存到检查点。这样可以：
- 节省磁盘空间
- 简化检查点管理
- 避免版本混淆

### Q: 如何验证只加载了 DA3 教师模型？
**A**: 查看训练日志的初始化部分，确认：
1. 出现 "使用 Depth Anything V3 作为教师模型"
2. 出现 "不会创建 pre_depth_encoder/pre_depth_decoder"
3. 出现 "不会创建 depth_anything_teacher (V1/V2)"

### Q: 如果中途恢复训练，教师模型会怎样？
**A**: 使用 `--load_weights_folder` 时：
- 学生模型从检查点加载
- 教师模型重新从 HuggingFace 加载（首次需下载，后续使用缓存）
- 训练继续进行，行为一致

---

## 总结

当前配置确保：
- ✅ 只有 `depth_anything_v3_teacher` 被加载
- ✅ `pre_depth_encoder`/`pre_depth_decoder` 不会被创建
- ✅ `depth_anything_teacher` (V1/V2) 不会被创建
- ✅ 教师模型不保存到检查点
- ✅ 教师模型不从检查点加载
- ✅ 代码逻辑清晰，三种教师模型互斥

这正是使用 Depth Anything V3 作为教师模型的正确配置！

